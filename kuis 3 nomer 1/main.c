/*Saya Muhammad Cahyana Bintang Fajar NIM 2102665 mengerjakan
 soal kuis 3 nomer 2 dalam mata kuliah Algoritma dan Pemrograman 1
untuk keberkahanNya maka saya tidak melakukan kecurangan seperti yang telah dispesifikasikan. Aamiin.*/
#include "header.h" //panggil header
int main()
{
    int n; // deklarasi n
    //scan
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%s", str[i]);
        panjang[i] = strlen(str[i]); // panjang tiap string langsung dimasukkan ke array agar bisa dicari maks
    }
    print(n); //panggil prosedur
    return 0; // reset kode
}
